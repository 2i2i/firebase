{
  "steps": [
   {
      "name": "gcr.io/$PROJECT_ID/firebase",
      "args": [
         "deploy",
         "--project",
         "$PROJECT_ID",
         "--only",
         "functions"
       ]
  },
  {
    "name": "gcr.io/$PROJECT_ID/firebase",
    "args": [
       "deploy",
       "--project",
       "$PROJECT_ID",
       "--only",
       "firestore:rules"
     ]
  }
  ]
}